import { SingleImageSectionHeroHome } from './SingleImageSectionHeroHome';

export const SingleImageSectionSeraph: React.FC = () => (
   <SingleImageSectionHeroHome
      titleContent="Seraph Station"
      descriptionContent="The Seraph Station project challenged us to design a unique station that would transport people through time. The result is a fresh and futuristic model inspired by space stations."
   />
);
